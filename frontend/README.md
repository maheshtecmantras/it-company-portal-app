# Frontend

This directory is a Create React App that services the IT company landing page as well as the login and registration flows.

## Commands

- `npm install`
- `npm start`
- `npm run build`

Set `REACT_APP_API_URL` (for example `http://localhost:8000/api`) before running in development or production so the forms can reach the Laravel backend.
